apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: smart-hostel
  labels:
    app: mongo
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    name: mongo
  selector:
    app: mongo
