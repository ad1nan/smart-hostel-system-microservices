apiVersion: v1
kind: Service
metadata:
  name: rooms
  namespace: smart-hostel
  labels:
    app: rooms
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    name: rooms
  selector:
    app: rooms
