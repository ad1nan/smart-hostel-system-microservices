apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: smart-hostel
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    name: api-gateway
  selector:
    app: api-gateway
