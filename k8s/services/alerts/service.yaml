apiVersion: v1
kind: Service
metadata:
  name: alerts
  namespace: smart-hostel
  labels:
    app: alerts
spec:
  type: ClusterIP
  ports:
  - port: 5003
    targetPort: 5003
    name: alerts
  selector:
    app: alerts
